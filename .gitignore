node_modules/
.next/
.env*.local
!.env.example
npm-debug.log*

# BUSY API reference material (docs, Postman collection, sample code, PDFs) — never commit
Integration using BUSY Web Service/

# local env
.env

# BUSY SQL discovery output — contains real business data samples
scripts/busy-sql/discovery-report.json

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# BUSY sync dry-run output and local state — real business data
local-data/

# Local-only BUSY schema notes — contains real purchase-price samples, never commit
docs/busy-schema-report.md

# npm cache (was committed by mistake in the initial commit)
.npm-cache/
